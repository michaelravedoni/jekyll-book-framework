'use strict';

module.exports = function(script) {
  var REGEX_VARIABLE = /(\{.+\})/;
  var result = REGEX_VARIABLE.exec(script);

  if(result) {
    throw new Error('NPM script expected ' + result[ 1 ] + ' but it was not defined.');
  }
};